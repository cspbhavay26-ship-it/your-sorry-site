<!doctype html>
<html lang="hi">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Madam Ji — एक दिल से माफ़ी</title>

  <!-- Devanagari-friendly typography -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@500;600&family=Poppins:wght@300;400;600&display=swap" rel="stylesheet">

  <style>
    :root{
      --primary: #967BB6; /* Lavender purple accent for buttons/heart */
      --bg: #E6E6FA;      /* Lavender background */
      --ink: #2b2b2b;     /* High-contrast body text */
      --muted: #6b7280;
      --card: #ffffff;
      --ring: color-mix(in oklab, var(--primary) 35%, #ffffff);
    }

    *{box-sizing:border-box}
    html,body{
      margin:0; padding:0;
      background: linear-gradient(180deg, var(--bg) 0%, #FFFFFF 100%);
      color: var(--ink);
    }
    body{
      font-family:Poppins,system-ui,-apple-system,Roboto,Helvetica,Arial,sans-serif;
      line-height:1.7;
      -webkit-font-smoothing:antialiased; -moz-osx-font-smoothing:grayscale;
    }

    /* Hero */
    .hero{
      min-height:100dvh; display:grid; place-items:center; position:relative; overflow:hidden;
      padding:clamp(16px, 4vw, 48px);
    }
    .hero-inner{
      max-width: 980px; text-align:center;
      background: color-mix(in oklab, var(--card) 85%, var(--primary));
      border:1px solid color-mix(in oklab, var(--primary) 12%, #0000);
      padding: clamp(20px, 5vw, 56px); border-radius:28px; backdrop-filter: blur(8px);
      box-shadow: 0 10px 40px color-mix(in oklab, #000 10%, var(--primary) 0%);
    }
    h1{
      font-family:"Playfair Display",serif; font-weight:600;
      font-size: clamp(28px, 6vw, 56px); margin:0 0 8px;
    }
    .lead{ color:var(--muted); font-size:clamp(14px, 2.6vw, 18px); margin:0 0 20px }

    /* Pulsing heart backdrop */
    .heart-bg{
      position:absolute; inset:auto -10% -20% -10%; height:60dvh; pointer-events:none; opacity:.08; filter: blur(2px);
      display:grid; place-items:center;
    }
    .heart{
      height:160px; width:160px; background: var(--primary); transform: rotate(-45deg); position:relative;
      animation: heartbeat 1.2s ease-in-out infinite;
    }
    .heart:before,.heart:after{ content:""; position:absolute; background: var(--primary); border-radius:50% }
    .heart:before{ width:160px; height:160px; top:-80px; left:0 }
    .heart:after { width:160px; height:160px; left:80px; top:0 }
    @keyframes heartbeat{
      0%{transform: rotate(-45deg) scale(1)}
      25%{transform: rotate(-45deg) scale(1.18)}
      45%{transform: rotate(-45deg) scale(1.32)}
      70%{transform: rotate(-45deg) scale(1.18)}
      100%{transform: rotate(-45deg) scale(1)}
    }

    /* Buttons */
    .btn{
      display:inline-block; padding:14px 22px; border-radius:999px;
      border:1px solid color-mix(in oklab, var(--primary) 40%, #0000);
      background: linear-gradient(180deg, color-mix(in oklab, var(--primary) 20%, #fff), #fff);
      color:#222; text-decoration:none; font-weight:600;
      box-shadow:0 8px 20px color-mix(in oklab, var(--primary) 30%, #0000);
      transition: transform .15s ease, box-shadow .15s ease;
    }
    .btn:hover{ transform: translateY(-2px); box-shadow:0 12px 26px color-mix(in oklab, var(--primary) 40%, #0000) }
    .btn.primary{ background: var(--primary); color:#fff; border-color:var(--primary) }

    /* Sections */
    .section{ padding: clamp(28px,6vw,80px) clamp(16px,5vw,48px) }
    .card{
      max-width: 980px; margin:0 auto; background:var(--card); border-radius:24px; padding:clamp(20px,4vw,40px);
      border:1px solid color-mix(in oklab, var(--primary) 12%, #0000);
      box-shadow:0 10px 30px color-mix(in oklab, #000 8%, #0000);
    }

    /* Typography */
    h2{ font-family:"Playfair Display",serif; margin:0 0 10px; font-size:clamp(22px,4.5vw,36px) }
    p{ margin:10px 0; color:#374151; font-size:clamp(15px,2.6vw,18px) }
    .small{ color:var(--muted); font-size:14px }

    /* Gallery */
    .grid{ display:grid; gap:14px; grid-template-columns: repeat(2, 1fr) }
    @media(min-width:760px){ .grid{ grid-template-columns: repeat(3, 1fr) } }
    .tile{
      position:relative; border-radius:14px; overflow:hidden; aspect-ratio:4/5; background:#f3f4f6; border:1px solid #eee;
      transform: translateZ(0);
    }
    .tile img{ width:100%; height:100%; object-fit:cover; display:block; transition: transform .6s ease, filter .6s ease }
    .tile:hover img{ transform: scale(1.06); filter:saturate(1.06) }
    .caption{
      position:absolute; left:10px; bottom:10px; background: color-mix(in oklab, #000 50%, #0000); color:#fff;
      padding:6px 10px; border-radius:999px; font-size:12px; backdrop-filter: blur(2px)
    }

    /* Reveal on scroll */
    .reveal{ opacity:0; transform: translateY(12px); transition: opacity .6s ease, transform .6s ease }
    .reveal.show{ opacity:1; transform:none }

    /* Footer mini */
    footer{ text-align:center; color:#6b7280; font-size:12px; padding:20px }
  </style>
</head>

<body>

  <!-- HERO -->
  <section class="hero">
    <div class="heart-bg"><div class="heart"></div></div>
    <div class="hero-inner reveal">
      <h1>Hey Madam Ji — माफ़ कर दो</h1>
      <p class="lead">यह पूरी वेबसाइट बस तुम्हें मुस्कुराने के लिए है, उसी मुस्कान के लिए जो दुनिया रोक देती है ❤️</p>
      <div class="cta">
        <a href="#letter" class="btn primary">दिल से लिखा</a>
        <a href="#gallery" class="btn">हमारी यादें</a>
      </div>
    </div>
  </section>

  <!-- LETTER -->
  <section id="letter" class="section">
    <div class="card reveal">
      <h2>दिल से, तुम्हारे लिए</h2>
      <p>
        Pata hai kya madam ji, Meri mummy hamesha kehti hai kabhi bhi kisi ka dil nahi dukhana chahiye, khaas kar jise aap pasand karte Hain unka To kabhi nahi, aur Maine apni bewakoof baton se tera, jise main sabse jyada pasand Karta Hun Uska dil bhi dukhaya aur rulaya bhi aur yeh meri zindagi ki sabse badi galti thi, toh main kitne bhi sorry bolu, sab kam hai par itna vaada karta hun teri aankho se ek aansoo bhi nahi aane dunga. Jab tu royi na ... Mere liye toh duniya hu ruk gayi thi.
        <br><br>
        Main perfect nahi hun par tere liye better chahta hun, yeh chahta hun ki hamara bitaya hua har ek pal itna suhana ki tujhe hamesha yaad rahe.
        <br><br>
        Bas ek mauka aur...
        <br>
        taaki is baar tere aansoon meri wajah na ho,
        <br>
        Meri wajah sirf teri hasi ho(Jo ki bhot cute hai 🥰🥰)
        <br><br>
        Aakhri main yahi kehna chahunga
        <br>
        Teri hasi ke liye, main waqt bhi rok doon,
        <br>
        Tumhari Khushi ke liye apni har galtiya mita doon,
        <br>
        Tum ho ek sapna, ek sachai jisse har pal main apna bana loon.
        <br><br>
        Toh Madam Ji.. in short jitni pyaari ho aap, jitne mood swings hai aapke, matlab jo aap ho na, sach main keher ho,
        <br>
        Keher Dhaa Rakha hai aapne 🥰🥰💗
      </p>

      <div class="cta" style="margin-top:14px">
        <button id="forgiveBtn" class="btn primary">Forgive me?</button>
        <a href="#gallery" class="btn">तुम्हारी तस्वीरें</a>
      </div>
    </div>
  </section>

  <!-- GALLERY -->
  <section id="gallery" class="section">
  <div class="card reveal" style="text-align:center;">
    <h2 style="font-family:'Playfair Display',serif;margin:0 0 18px">Dil Se Maafi ❤️</h2>
    <div style="margin:40px 0;">
      <div class="heart" style="margin:0 auto; width:120px; height:120px; animation:heartbeat 1.1s ease-in-out infinite;"></div>
      <p style="margin-top:30px;font-size:22px;color:#967bb6;">कभी-कभी एक दिल से Sorry दिल को छू जाता है।</p>
    </div>
    <div class="cta">
      <button id="forgiveBtn" class="btn primary">Maaf Kar Do?</button>
    </div>
  </div>
</section>


  <footer>
    Made with प्यार — theme in Lavender for a calm, romantic mood. 
  </footer>

  <script>
    /* Optional: override theme color from URL, e.g., ?color=%23967BB6 */
    (function(){
      const url = new URL(window.location.href);
      const c = url.searchParams.get("color");
      if(c){ document.documentElement.style.setProperty('--primary', c); }
    })();

    /* Reveal on scroll */
    const reveals = document.querySelectorAll('.reveal');
    const io = new IntersectionObserver((entries)=>{
      entries.forEach(e=>{ if(e.isIntersecting){ e.target.classList.add('show'); }});
    },{threshold:.15});
    reveals.forEach(r=>io.observe(r));

    /* Lightweight confetti on forgiveness */
    function confettiSimple(x,y,count=120, colors){
      const canvas = document.createElement('canvas');
      const ctx = canvas.getContext('2d');
      const dpr = window.devicePixelRatio || 1;
      canvas.style.position='fixed';
      canvas.style.left='0'; canvas.style.top='0'; canvas.style.pointerEvents='none';
      canvas.width = innerWidth * dpr; canvas.height = innerHeight * dpr;
      canvas.style.width = innerWidth+'px'; canvas.style.height = innerHeight+'px';
      document.body.appendChild(canvas);

      const parts=[];
      const palette = colors || ['#ffffff','var(--primary)','rgba(0,0,0,.12)','#ffd1dc','#ffe4e1'];
      for(let i=0;i<count;i++){
        parts.push({
          x: x*dpr, y: y*dpr,
          vx: (Math.random()*2-1)*6*dpr,
          vy: (Math.random()*-1)*10*dpr - 6*dpr,
          size: Math.random()*6+4,
          rot: Math.random()*Math.PI,
          vr: (Math.random()*2-1)*.2,
          col: palette[Math.floor(Math.random()*palette.length)],
          life: 0, max: 220 + Math.random()*80
        });
      }

      const g = 0.25*dpr, drag=0.995;
      function tick(){
        ctx.clearRect(0,0,canvas.width,canvas.height);
        parts.forEach(p=>{
          p.life++;
          p.vx*=drag; p.vy = p.vy*drag + g;
          p.x += p.vx; p.y += p.vy; p.rot += p.vr;
          ctx.save();
          ctx.translate(p.x,p.y);
          ctx.rotate(p.rot);
          ctx.fillStyle = p.col;
          ctx.fillRect(-p.size/2,-p.size/2,p.size,p.size);
          ctx.restore();
        });
        for(let i=parts.length-1;i>=0;i--){
          if(parts[i].life>parts[i].max || parts[i].y>canvas.height+40){ parts.splice(i,1); }
        }
        if(parts.length){ requestAnimationFrame(tick); } else { canvas.remove(); }
      }
      tick();
    }

    document.getElementById('forgiveBtn').addEventListener('click', (e)=>{
      const rect = e.target.getBoundingClientRect();
      confettiSimple(rect.left + rect.width/2, rect.top + window.scrollY);
      e.target.textContent = 'Thank you ❤️';
    });
  </script>
</body>
</html>
